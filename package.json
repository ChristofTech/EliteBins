{
  "name": "elite-bins-elm",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "start": "elm make src/Main.elm --output=elm.js && python3 -m http.server 8000",
    "build": "elm make src/Main.elm --output=elm.js --optimize",
    "dev": "elm make src/Main.elm --output=elm.js && python3 -m http.server 8000"
  },
  "dependencies": {
  },
  "devDependencies": {
  }
}
