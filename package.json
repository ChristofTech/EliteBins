{
  "name": "elite-bins-elm",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "npx elm make src/Main.elm --output=elm.js && python3 -m http.server 8000",
    "build": "npx elm make src/Main.elm --output=elm.js --optimize",
    "preview": "vite preview",
    "elm:make": "npx elm make src/Main.elm --output=elm.js",
    "elm:dev": "npx elm make src/Main.elm --output=elm.js --debug"
  },
  "dependencies": {},
  "devDependencies": {
    "elm": "0.19.1"
  }
}